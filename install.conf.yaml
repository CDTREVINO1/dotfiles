- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.tmux.conf: tmux.conf
#    ~/.vim: vim
#    ~/.vimrc: vimrc
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Work

- shell:
    - 
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
